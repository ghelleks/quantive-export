# Copy this file to .env.test and fill in real values
# NEVER commit .env.test file!

# Real Quantive API credentials for testing
QUANTIVE_API_TOKEN=your_test_api_token_here
QUANTIVE_ACCOUNT_ID=your_test_account_id_here
QUANTIVE_BASE_URL=https://api.quantive.com/v1

# Test session identifiers (existing sessions in your test account)
TEST_SESSION_NAME=your_test_session_name
TEST_SESSION_UUID=your_test_session_uuid
INVALID_SESSION_NAME=nonexistent_session_name

# Test control flags
SKIP_API_TESTS=true
API_TEST_TIMEOUT=10000

# Instructions:
# 1. Copy this file: cp .env.test.example .env.test
# 2. Replace placeholder values with real test credentials
# 3. Use a dedicated test Quantive account (not production!)
# 4. Verify test sessions exist in your account
# 5. Set SKIP_API_TESTS=false to enable integration tests
